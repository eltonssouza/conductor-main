__pycache__/
*.py[cod]

# RAG vector index — built locally
conductor/rag/chroma/
conductor/rag/*.log

# Honcho self-host secrets (the live .env is staged to ~/.claude/conductor/infra)
conductor/infra/honcho/.env

# Python build/editable-install artifacts
*.egg-info/
build/
dist/

# Books corpus archive — large; kept local, not committed (specific file, not *.7z)
to-brain.7z
